# Core dependencies for training/evaluation (rgb_grains.train, rgb_grains.pipeline, rgb_grains.viz.eda).
# For raw hyperspectral segmentation (rgb_grains.segmentation), also install
# rgb_grains/segmentation/requirements.txt, or `pip install -e ".[segmentation]"`.
torch>=2.0
numpy>=1.23
pandas>=1.5
scikit-learn>=1.1
seaborn>=0.12
matplotlib>=3.5
tqdm>=4.64
tabulate>=0.9
